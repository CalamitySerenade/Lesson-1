//Today's objective is to go over JS for loops, arrays, and for each
let array=[1,2,3,4,5,6];
//length=6, index=5
console.log(array);

//Basic notes:
//1. Array index always starts at 0
//2. FormulaIndex= Array.length-1

//Adds 2 to the original values
for(let i=0;i<array.length;i++){
    console.log(array[i]+2);
}

let array2=[1,2,3];
for